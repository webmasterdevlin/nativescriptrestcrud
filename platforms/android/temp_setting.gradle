android {
            defaultConfig {
                applicationId = "org.nativescript.NativeScriptRESTCRUD"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }